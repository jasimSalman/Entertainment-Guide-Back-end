const express = require('express')
const router = express.Router()

const userCtrl = require('../controllers/users')

// router.post('/login/:id', userCtrl.login)

// router.post('/signup/:id', userCtrl.signup)

module.exports = router
